<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Form Page</title>
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <div class="w-full min-h-screen bg-zinc-900 p-10 text-white ">

    <div class="nav mb-10">
        <a class="text-blue-500" href="/read">Read Users</a>
    </div>
 <h1 class="text-3xl mb-3 tracking-tighter">Update User</h1>
    
    <form action="/update/<%=user._id %>" method="POST" class="w-full max-w-md space-y-4">
           
      <input
        class="px-5 py-3 border-2 border-zinc-400 bg-transparent outline-none w-full rounded"
        type="text"
        placeholder="Name"
        name="name"
        value="<%= user.name %>">

      <input
        class="px-5 py-3 border-2 border-zinc-400 bg-transparent outline-none w-full rounded"
        type="email"
        placeholder="Email"
        name="email"
        id="email"
        value="<%= user.email %>">

      <input
        class="px-5 py-3 border-2 border-zinc-400 bg-transparent outline-none w-full rounded"
        type="text"
        placeholder="Image URL"
        name="image"
        id="image"
        value="<%= user.image %>">

      <button
        class="px-5 py-3 bg-yellow-600 hover:bg-yellow-500 rounded-lg w-full transition "
        type="submit" value="Update User"
      >
        Submit
      </button>
    </form>
  </div>
</body>
</html>
