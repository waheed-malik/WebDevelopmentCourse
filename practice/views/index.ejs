<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>All Posts</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans">

  <div class="max-w-5xl mx-auto p-6">
    <!-- Page Title -->
    <h1 class="text-4xl font-bold text-center text-indigo-700 mb-8">📰 My Blog Posts</h1>

    <!-- Add New Post Button -->
    <div class="text-center mb-8">
      <a href="/new" class="inline-block bg-green-600 text-white px-6 py-2 rounded shadow hover:bg-green-700 transition">
        ➕ Add New Post
      </a>
    </div>

    <!-- Blog Posts -->
    <% if (posts.length === 0) { %>
      <p class="text-center text-gray-600">No posts yet. Start by adding one!</p>
    <% } else { %>
      <div class="grid md:grid-cols-2 gap-6">
        <% posts.forEach(post => { %>
          <div class="bg-white rounded-lg shadow p-6 hover:shadow-md transition">
            <h2 class="text-2xl font-semibold text-indigo-800 mb-2"><%= post.title %></h2>
            <p class="text-gray-700 mb-4"><%= post.content %></p>
            <p class="text-sm text-gray-500">🗓️ <%= post.createdAt.toDateString() %></p>
          </div>
        <% }) %>
      </div>
    <% } %>
  </div>

</body>
</html>
